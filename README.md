# afb-yolo13

Baseline + architectural improvement experiments untuk **deteksi *Mycobacterium tuberculosis* (AFB / bacilli)** dengan **YOLOv13** pada dataset **Tuberculosis6208** (phone-camera microscopy, Makerere).

## Dataset

- **Source:** [Tuberculosis6208 — Makerere Automated Lab Diagnostics Database](https://zenodo.org/record/...) (Pascal-VOC format).
- **N pairs:** 1265 image+XML.
- **Class:** 1 (`bacilli`, mapped dari label XML `TBbacillus`).
- **Split:** Chen et al. IJAI 2024 — **1024 train / 140 val / 101 test**, deterministic shuffle `seed=42` (sama dengan eksperimen YOLOv12 untuk apples-to-apples comparison).
- **Local path:** `D:/project/yolov12/Tuberculosis6208/tuberculosis-phonecamera/`

## Repo layout

```
afb-yolo13/
├── README.md
├── requirements.txt
├── .gitignore
├── configs/
│   └── data.yaml.tmpl          # template, generated by build_chen_split.py
├── scripts/
│   ├── build_chen_split.py     # VOC → YOLO + 1024/140/101 split, seed=42
│   ├── train_baseline.py       # YOLOv13s baseline, seed=42, mirror yolo12 hyperparams
│   └── diagnose_baseline.py    # per-IoU mAP, FP composition, FullPAD gate probe
└── notebooks/
    └── train_baseline.ipynb    # interactive version
```

## Setup

YOLOv13 fork (iMoonLab) harus sudah di-install editable. Asumsi fork ada di `D:/Project/yolov13/`:

```powershell
# (sekali saja) install YOLOv13 dari local fork
pip install -e D:/Project/yolov13

# install dependency tambahan
pip install -r requirements.txt
```

## Workflow baseline (seed=42, 1 run)

### 1. Build split

```powershell
python scripts/build_chen_split.py `
    --src "D:/project/yolov12/Tuberculosis6208/tuberculosis-phonecamera" `
    --out "D:/datasets/tb_chen_split" `
    --seed 42
```

Output:
```
Image+XML pairs: 1265 (target 1265)
Split: train=1024  val=140  test=101  seed=42
Wrote D:/datasets/tb_chen_split/data.yaml
```

### 2. Train baseline YOLOv13s

```powershell
python scripts/train_baseline.py `
    --data "D:/datasets/tb_chen_split/data.yaml" `
    --model yolov13s `
    --seed 42 `
    --epochs 60 `
    --imgsz 640 `
    --batch 16 `
    --project "D:/runs/afb_yolov13" `
    --name yolov13s_seed42_60ep
```

Pretrained `yolov13s.pt` auto-download dari iMoonLab release.

### 3. Diagnose baseline (untuk decide arsitektur improvement)

```powershell
python scripts/diagnose_baseline.py `
    --ckpt "D:/runs/afb_yolov13/yolov13s_seed42_60ep/weights/best.pt" `
    --data "D:/datasets/tb_chen_split/data.yaml" `
    --split val `
    --imgsz 640
```

Output:
- per-IoU mAP (0.5, 0.6, 0.7, 0.8, 0.9)
- FP composition (near / close / far / would-be-TP)
- FullPAD_Tunnel gate values (cek HyperACE pathway aktif atau α-trap)
- per-scale recall (P3/P4/P5)
- recommendation tag (e.g., "fix-localization", "fix-fp", "arch-add-p2")

## Experiments

### v1 — Baseline (`yolov13s.yaml`)

```powershell
# di notebook, pilih:
MODEL_CFG = 'yolov13s.yaml'
```

| Split | mAP50 | mAP50-95 | mAP@0.9 | P | R |
|---|---|---|---|---|---|
| val | 0.835 | 0.415 | — | 0.762 | 0.767 |
| test | 0.877 | 0.428 | 0.003 | 0.816 | 0.786 |

Diagnose flags (baseline val):
- **HIGH** `LOSS_LOCALIZATION` — mAP drop 0.117 dari IoU 0.5 → 0.7. Banyak prediksi tidak presisi (mAP@0.9 ≈ 0).
- **HIGH** `HARD_NEG_FP` — 66.9% FPs jauh dari GT (sebagian besar kemungkinan label noise, lihat label-quality probe).
- FullPAD gates: **HEALTHY** (mean |g| = 0.152, 0/7 dead) → tidak perlu redesign HyperACE pathway.

### v2 — P2 detection head ([`configs/yolov13s-p2.yaml`](configs/yolov13s-p2.yaml))

Diagnose-driven response ke `LOSS_LOCALIZATION` flag:
- Tambah detection head di stride 4 (feature map 160×160 @ imgsz 640) → resolusi lebih tinggi untuk small bacilli.
- FPN top-down diperpanjang P5 → P4 → P3 → **P2** (cat backbone P2 dari layer 2).
- PAN bottom-up dimulai dari P2 → P3 → P4 → P5 (refresh semua skala).
- HyperACE + FullPAD path **tidak diubah** (gates sehat, jangan ganggu).

```python
MODEL_CFG = str(REPO_DIR / 'configs' / 'yolov13s-p2.yaml')
```

Pretrained `yolov13s.pt` di-load partial:
- ✓ backbone (layer 0-8)
- ✓ HyperACE + FullPAD distribution (layer 9-14)
- ✓ FPN P3/P4/P5 path (layer 15-23)
- ✗ P2 extension (layer 24-26) — init dari scratch
- ✗ PAN bottom-up (layer 27-37) — init dari scratch
- ✗ Detect head 4-output — init dari scratch (mismatch dgn yolov13s.pt 3-output)

Expected cost: ~+10–15% params, ~+30–40% FLOPs, ~+25–35% train time vs baseline.

### Future variants (TBD)

- v3 — `yolov13s-p2-nwd.yaml`: P2 head + NWD loss (`nwd_ratio=0.5`) untuk localization gain lebih lanjut.
- v4 — Hyperedge scale-conditioned (eksperimen arsitektur HyperACE itu sendiri).

Detail per-eksperimen tracked di W&B project `afb_yolov13_chen`, plus diagnose JSON per-run di `diag_<run_name>_val/`.
